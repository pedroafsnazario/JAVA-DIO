 public class TiposVariaveis {
    public static void main(String[] args) throws Exception {
   /*  
     Métodos ->
     uma classe é definida por atributos e métodos, já foi visto que atributos são, em sua grande maioria,
     váriaveis de diferentes tipos e valores. Os métodos em sua vez, correspondem a funções ou sub-rotinas
     disponiveis dentro de nossas classes.

     Critérios a seguir (por questões de organizar e facilitar a leitura de código) ->

     - Deve ser nomeado como verbo
     - Seguir padrão camelCase

     exemplos ->

     somar (int n1, int n2) {}

     abrirConexao() {}

     concluirProcessamento() {}

     findById(int id) {}

     -> Equivocado!

     calcularImprimir() {} --> como assim "calcular Imprimir"? um metodo consegue fazer essas duas coisas distantes?
     o responsavel a se fazer é criar dois metodos um pra calculo e outro pra impressao e ir vinculando os dois
     caso seja nescessario o uso de ambos ao mesmo tempo.

     O responsável é fazer método que realizem apenas um coisa, e nao duas!

     Critério para Definir um Método ->

     - Qual a proposta principal do método?

     - Qual o tipo de retorno esperado após executar o método?

     (caso um método não retorne um valor, ele será representado pela palavra chave 'void').
     (ou seja, não terá de colocar a função 'return' no método :]).

     - Quais parametros serão nescessários para execução do método?

     - O método possui o risco de apresentar alguma exceção?

     - Qual a visibilidade do método? (private/public)
   */

    }
}
